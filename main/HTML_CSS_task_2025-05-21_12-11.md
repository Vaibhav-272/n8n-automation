TML & CSS: Your First Steps to Web Development
hat is HTML?
ML (HyperText Markup Language) is the foundation of every website, providing structure and content. Think of it as the skeleton of a webpage. It uses tags to define elements like headings, paragraphs, images, and links.
xample:**
tml
OCTYPE html>
tml>
ead>
itle>My First Webpage</title>
ead>
ody>
1>Hello, World!</h1>
This is my first paragraph.</p>
ody>
tml>
xplanation:**
OCTYPE html>`: Declares the document type as HTML5.
tml>`: The root element of the page.
ead>`: Contains meta-information about the HTML document, such as the title.
itle>`: Specifies a title for the HTML page (which is shown in the browser's title bar or tab).
ody>`: Contains the visible page content.
1>`: Defines a large heading.
Defines a paragraph.
nderstanding HTML Tags
ML uses tags to define elements. Most tags come in pairs: an opening tag and a closing tag.
pening tag: `<tagname>`
losing tag: `</tagname>`
ommon HTML Tags:**
1>` to `<h6>`: Headings (different sizes)
Paragraph
Link (anchor)
mg>`: Image
l>` and `<li>`: Unordered list and list item
l>` and `<li>`: Ordered list and list item
iv>`: Division or section
pan>`: Inline container
xample:**
tml
href="https://www.example.com">Visit Example</a>
mg src="image.jpg" alt="My Image">
hat is CSS?
S (Cascading Style Sheets) is used to style HTML elements. It controls the look and feel of your website, including colors, fonts, layout, and responsiveness. Think of it as the clothing and makeup for your webpage.
xample:**
ss
olor: blue;
ext-align: center;
ont-family: Arial, sans-serif;
ont-size: 16px;
is CSS code will:
ake all `<h1>` headings blue and centered.
et the font for all `<p>` paragraphs to Arial or a generic sans-serif font, and set the font size to 16 pixels.
ow to Add CSS to HTML
ere are three ways to add CSS to your HTML:
nline CSS:** Directly within HTML tags (not recommended for large projects).
tml
1 style="color: green;">This is a green heading</h1>
nternal CSS:** Within the `<style>` tag in the `<head>` section.
tml
OCTYPE html>
tml>
ead>
itle>My Webpage</title>
tyle>
1 {
olor: green;
tyle>
ead>
ody>
1>This is a green heading</h1>
ody>
tml>
xternal CSS:** In a separate `.css` file, linked to the HTML document. This is the best practice for larger projects.
TML (`index.html`):**
tml
OCTYPE html>
tml>
ead>
itle>My Webpage</title>
ink rel="stylesheet" href="style.css">
ead>
ody>
1>This is a green heading</h1>
ody>
tml>
SS (`style.css`):**
ss
1 {
olor: green;
SS Selectors
S selectors are used to target the HTML elements you want to style.
lement Selector:** Selects elements based on the element name. (e.g., `p`, `h1`, `div`)
D Selector:** Selects elements based on their ID attribute. (e.g., `#my-heading`)
lass Selector:** Selects elements based on their class attribute. (e.g., `.my-paragraph`)
xample:**
tml
OCTYPE html>
tml>
ead>
itle>CSS Selectors</title>
tyle>
1 {
olor: blue; /* Element selector */
nique-paragraph {
olor: red; /* ID selector */
ighlight {
ackground-color: yellow; /* Class selector */
tyle>
ead>
ody>
1>This is a heading</h1>
id="unique-paragraph">This is a unique paragraph.</p>
class="highlight">This paragraph is highlighted.</p>
This is a normal paragraph.</p>
ody>
tml>
asic CSS Properties
re are some essential CSS properties:
olor`: Text color
ont-family`: Font type
ont-size`: Font size
ackground-color`: Background color
idth`: Element width
eight`: Element height
adding`: Space between content and border
argin`: Space outside the border
ext-align`: Text alignment (left, center, right)
xample:**
ss
dy {
ont-family: Arial, sans-serif;
ackground-color: #f0f0f0; /* Light gray */
ontainer {
idth: 80%;
argin: 0 auto; /* Centers the container */
adding: 20px;
ackground-color: white;
ext Steps
is is just the beginning! To continue your learning journey:
ractice writing HTML and CSS code regularly.
xplore online resources like MDN Web Docs and CSS-Tricks.
uild small projects to apply what you've learned.
xperiment with different CSS properties and selectors.
ppy coding!